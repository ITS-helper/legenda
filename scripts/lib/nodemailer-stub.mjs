// Заглушка nodemailer для локального предпросмотра: письма из скриптов не отправляются.
export default {
  createTransport() {
    throw new Error('Отправка писем недоступна в локальном предпросмотре')
  },
}
